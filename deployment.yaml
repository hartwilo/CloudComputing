apiVersion: apps/v1
kind: Deployment
metadata:
  name: CloudComputing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: CloudComputing
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: CloudComputing
    spec:
      containers:
      - name: helloUser-app
        image: gcr.io/PROJECT_ID/IMAGE:TAG
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
